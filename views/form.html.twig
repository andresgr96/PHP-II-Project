<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link href="css/bootstrap.min.css" rel="stylesheet" type="text/css">
    <link href="css/styles.css" rel="stylesheet" type="text/css">
    <title>Word Frequency</title>
  </head>
  <body>
    <div class="container">
      <h1>Find out how many times does your word appear in a sentence!</h1>
    </h3>Just input a word and a sentence and see the magic!</h3>
      <div class="row">
        <form action="/result" method="post">
          <div class="col-xs-6">
            <div class="form-group">
              <label for="word_input">Please input a word:</label>
              <input type="text" name="word_input" id="word_input" class="form-control" required>
            </div>
          </div>
          <div class="col-xs-6">
            <div class="form-group">
              <label for="sentence">Please input a sentence:</label>
              <input type="text" name="sentence" id="sentence" class="form-control" required>
            </div>
          </div>
          <button type="submit" class="btn btn-primary">Submit</button>
        </form>
        {% if count.getCount is not empty %}
          <h3>{{ word }} appears {{ count.getCount }} times!</h3>
        {% endif %}
      </div>
    </div>
  </body>
</html>
